I use vite-plus for node (`vp` for package manger, `vpx` for replacing npx and `vpr` for `npm run`) which uses the correct package manager, `node` automatically resolves to correct version.
uv and uvx for python needs, standard setup
My browser of choice is Helium, there are other browsers installed but use Helium unless asked for a different one.
For github related things perfer to use the `gh` cli as its most likely to have auth.

Always assume changes are breaking unless specified to maintain compatibility, everything is greenfield or exploration, don't be afraid to break things. avoid single use slop functions like `isRecord` or `isThing`. trust typescript types, use a validation library when needed instead of inventing your own. Don't be eager to write/change files unless I am specifically asking you for changes, most of the time my first message is asking your opinion on things
